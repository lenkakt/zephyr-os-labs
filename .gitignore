# Zephyr build output (regenerated by `west build`)
/build/
/*/build/

# The west workspace itself, when checked out alongside this repo rather
# than symlinked from a shared classroom install (see README.rst).
/.west/
/bootloader/
/modules/
/tools/
/zephyr/
